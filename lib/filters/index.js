const html = require('./html');
const js = require('./js');
const css = require('./css');
const font = require('./font');
const image = require('./image');

module.export = {
  html: html,
  js: js,
  css: css,
  font: font,
  image: image
}
